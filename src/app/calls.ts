export interface Calls {
    Kontra: boolean;
    Re: boolean;
    NoX: number; //  (1 = keine 90, 2 = keine 60, 3 = keine 30, 4 = Schwarz)
    LowTrump: boolean;
    Solo: number; // (1 = buben, 2 = Damen, 3 = Trumpf solo)    
    Hochzeit: number; // 1 = Fehlstich, 2 = Egalstich
    Fuchsjagd: boolean;

  }

